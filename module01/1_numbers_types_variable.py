a = 2
b = 3
c = 31.4
d = 2.0 + 2j
e = "31.4"
f = "Hello World"
g = "Привіт Привет"
h = True
k = None
_ = None
print(a, b)  # int (numbers)
print(type(a))  # int type
print(type(c))  # float types
print(type(d))  # complex type (numbers)
print(type(e))  # str type (string with numbers)
print(type(f))  # str type (string with text English)
print(type(g))  # str type (string with text Ukrainian, Russian)
print(type(h))  # bool type (string with bool (True/False))
print(type(k))  # NoneType type (only None)
print(type(_))  # NoneType type (only None)

# 1add = 4  # Not correct, because first number
# Anton = "Anton"  # Not correct, because first char letter is uppercase
# continue = "Anton"  # Not correct, because reserved name
# anton 1 = "Anton"  # Not correct, because we have space in variable
# _anton = "Anton"  # Correct, because _ is normal
# anton_1 = "Anton"  # Correct, because first char is letter
