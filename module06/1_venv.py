# venv virtual environment
# py(python) -m venv .venv -> create python module -m venv virual environment and name
# .venv/Scripts/activate.bat in cmd
# exit from venv use dectivate or deactivate.bat
# in VS Code we can use other environment(right and down)
# need create other environment for other project
import raise_salary as salary_module  # do all function in file raise_salary


print(salary_module.raise_salary(1000, 0))
